#include<bits/stdc++.h>

using namespace std;

int s1, s2, n;
int k[201];

int dp(int faltas1, int faltas2, ){

}


int main()
{
    cin.sync_with_stdio(false);
    cout.sync_with_stdio(false);

    int T;

    cin >> T;

    while(cin >> s1 >> s2){
        cin >> n;
        for(int i = 0 ; i < n ; ++i)cin>>k[i];
        dp(s1, s2, );
    }

    return 0;
}
