#include<bits/stdc++.h>

#define MAX (1<<30)

using namespace std;

int dp[100];

int main(){
	
	memset(dp, -1, sizeof(dp));
	
	dp[1] = 3;
	cout << MAX << endl;
	//for(int i = 1 ; i <= (1>>)
	
	
	return 0;
}
