#include<bits/stdc++.h>

using namespace std;

int main(){
	string s;
	int sz, limit, maxlen;
	
	cin >> s;
	sz = s.size();
	maxlen = limit = sz/3;
	
	for(int i ; i < limit && j < limit && k < limit; ){
		
	}
	
	return 0;
}
